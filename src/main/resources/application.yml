server:
  port: 8877
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: ROOT
    url: jdbc:mysql://localhost:3306/securet?createDatabaseIfNotExist=true
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
